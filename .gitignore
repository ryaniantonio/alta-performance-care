# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env: `.env` (publico) e `.env.example` sao versionados; segredos ficam em
# `.env.local` (ja coberto por *.local). Ignora qualquer outro `.env.*` por seguranca.
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — per-machine, NOT shared (the team baseline .claude/settings.json
# and .claude/rules/ ARE committed; only local state below is ignored)
.claude/settings.local.json
.claude/plans/
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/*.local
